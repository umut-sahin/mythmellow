//! Player constants.

use crate::prelude::*;


/// Base dash duration of the player.
pub const BASE_PLAYER_DASH_COOLDOWN_DURATION: Duration = Duration::from_millis(1500);

/// Base dash duration of the player.
pub const BASE_PLAYER_DASH_DURATION: Duration = Duration::from_millis(75);

/// Base health of the player.
pub const BASE_PLAYER_HEALTH: f32 = 10.00;

/// Base pickup range of the player.
pub const BASE_PLAYER_PICKUP_RANGE: f32 = 50.00;

/// Base speed of the player.
pub const BASE_PLAYER_SPEED: f32 = 200.00;
